--Scenario: Ranking Data

--CREATE TABLE Participants (ParticipantID SERIAL PRIMARY KEY,ParticipantName VARCHAR(100) NOT NULL,
--Score INT);

--SELECT 
    --ParticipantID,
    --ParticipantName,
    --Score,
    --RANK() OVER (ORDER BY Score DESC) AS Rank
--FROM Participants;



